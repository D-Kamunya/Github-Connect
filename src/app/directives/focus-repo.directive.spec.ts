import { FocusRepoDirective } from './focus-repo.directive';

describe('FocusRepoDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusRepoDirective();
    expect(directive).toBeTruthy();
  });
});
